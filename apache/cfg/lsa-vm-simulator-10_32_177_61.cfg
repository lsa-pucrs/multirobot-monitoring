# A simple configuration file for monitoring the local host
# This can serve as an example for configuring other servers;
# Custom services specific to this host are added here, but services
# defined in nagios2-common_services.cfg may also apply.
# 

define host{
        use                     generic-host            ; Name of host template to use
        host_name               lsa-vm-simulator-10_32_177_61
        alias                   lsa-vm-simulator-10_32_177_61
        address                 10.32.177.61
        }

# Define a service to check the disk space of the root partition
# on the local machine.  Warning if < 20% free, critical if
# < 10% free space on partition.

define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Disk Space
        check_command                   check_all_disks!20%!10%
        }



# Define a service to check the number of currently logged in
# users on the local machine.  Warning if > 20 users, critical
# if > 50 users.

define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Current Users
        check_command                   check_users!20!50
        }


# Define a service to check the number of currently running procs
# on the local machine.  Warning if > 250 processes, critical if
# > 400 processes.

define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Total Processes
		check_command                   check_procs!250!400
        }



# Define a service to check the load on the local machine. 

define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Current Load
		check_command           	check_load!5.0!4.0!3.0!10.0!6.0!4.0
        }


# Define a service to check the robot sensors status 

# All sensors
define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Robot all sensors
        check_command                   check-all
        }


# Battery
define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Robot / Laptop battery
        check_command                   check-battery
        }

		
# Laser
define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Robot Lasers
        check_command                   check-laser
        }

		
# Camera
define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Robot Camera
        check_command                   check-laser
        }

		
# Motor
define service{
        use                             generic-service         ; Name of service template to use
        host_name                       lsa-vm-simulator-10_32_177_61
        service_description             Robot Motors
        check_command                   check-motor
        }
